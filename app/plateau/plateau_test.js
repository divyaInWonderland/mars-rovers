'use strict';

describe('app.plateau module', function() {

  beforeEach(module('app.plateau'));

  describe('plateau controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var plateauCtrl = $controller('PlateauCtrl');
      expect(plateauCtrl).toBeDefined();
    }));

  });
});